const initialState = {
  cartItems: [], // Initial cart items state
};

const cartReducer = (state = initialState, action) => {
  switch (action.type) {
    // Add cases for different cart-related actions
    default:
      return state;
  }
};

export default cartReducer;
